{% extends "base.html.twig" %}

{% block body %}

<h1 class="text-2xl font-bold text-gray-900">Catégories</h1>

<div class="py-3 px-15 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    {% for categorie in categories %}
        <div class=" bg-indigo-300/30 p-4 w-full rounded-xl shadow-indigo-600/50 hover:shadow-lg hover:bg-indigo-200/30 text-xl font-semibold transition-all text-center">
            <a class="w-full h-full" href="{{ path('app_categorie', {id: categorie.id}) }}">{{ categorie.nom }}</a>
        </div>
    {% endfor %}
</div>

{% endblock %}